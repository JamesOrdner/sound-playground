#include "Auralizer.h"
#include "../Components/AuralizingAudioComponent.h"

void Auralizer::auralize()
{
	//for (auto it = auralizingComponents.cbegin(); it != auralizingComponents.cend(); it++) {
	//	const auto receivers = (*it)->indirectReceivers();
	//	for (auto rec = receivers.cbegin(); rec != receivers.cend(); rec++) {
	//		(*it)->updateIndirectIR(rec->receiver, { 0, 0, 1 });
	//	}
	//}
}
